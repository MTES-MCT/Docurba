#!/bin/sh

# Python buildpack (https://github.com/Scalingo/python-buildpack) doesn't allow to specify command line options when running uv
# We have to explicitly re-sync including deploy optional dependencies
uv sync --locked --no-default-groups --extra deploy

./manage.py check --deploy --fail-level WARNING
